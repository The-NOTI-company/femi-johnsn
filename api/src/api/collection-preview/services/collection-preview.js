'use strict';

/**
 * collection-preview service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::collection-preview.collection-preview');
